# ========================================
# 🌍 DJANGO CORE SETTINGS
# ========================================

# Debug mode (set to False in production)
DEBUG=True

# Django secret key — generate your own using:
# >>> from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())
DJANGO_SECRET_KEY=your_very_secret_django_key_here

# Allowed hosts (comma-separated)
# Add your production domain(s) or server IPs
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# ========================================
# 🗄️ DATABASE CONFIGURATION
# ========================================

# Supported engines: 
# - django.db.backends.postgresql
# - django.db.backends.mysql
# - django.db.backends.sqlite3
DB_ENGINE=django.db.backends.postgresql

# Database credentials
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Database host and port
DB_HOST=localhost
DB_PORT=5432

# ========================================
# 📧 EMAIL (SMTP) CONFIGURATION
# ========================================

# Email backend (default SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP server details (use Mailtrap, SendGrid, or Gmail)
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# SMTP credentials
EMAIL_HOST_USER=your_smtp_username
EMAIL_HOST_PASSWORD=your_smtp_password

# Default sender email
DEFAULT_FROM_EMAIL=Admin <admin@yourdomain.com>

# ========================================
# ☁️ CLOUDINARY CONFIGURATION (for media)
# ========================================

# Your Cloudinary account credentials
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ========================================
# 🔐 SECURITY & CORS SETTINGS
# ========================================

# Comma-separated list of allowed frontend origins
# Example: local frontend and live production app
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourfrontenddomain.com

# ========================================
# 🔑 JWT AUTHENTICATION SETTINGS
# ========================================

# Token lifetime configuration
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=14
